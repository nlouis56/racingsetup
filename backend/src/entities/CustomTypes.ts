export enum VehicleType {
    KART = 'kart',
    TRACK_CAR = 'track_car',
    ROAD_CAR = 'road_car',
    RALLY_CAR = 'rally_car',
}

export enum AuthType {
    EMAIL = 'email',
    GOOGLE = 'google',
}
